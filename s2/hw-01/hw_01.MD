### Создание ролей:


create role app login password 'app123';

create role readonly login password 'readonly123';

create role updater login password 'updater123';


### Выдача прав:

grant connect on database petshopdatabase to app, readonly, updater;

grant usage on schema petshopschema to app, readonly, updater;

grant select, update, delete, insert on all tables in schema petshopschema to app;

grant select on all tables in schema petshopschema to readonly;

grant update on all tables in schema petshopschema to updater;

### Проверка прав:

<img width="1280" height="720" alt="image" src="https://github.com/user-attachments/assets/c589c014-c728-4c51-a66f-2de7aaa05288" />

### Запуск Postgres через Docker Compose up -d:

<img width="1083" height="670" alt="image" src="https://github.com/user-attachments/assets/9521d552-fb64-4b57-91de-29c37da8bdc4" />